const keyValuePair = require('./utilities/keyValuePair');
const loop = require('./utilities/loop');
const loopToObject = require('./utilities/loopToObject');
const newElement = require('./utilities/newElement');

module.exports = {
  keyValuePair: keyValuePair,
  loop: loop,
  loopToObject: loopToObject,
  newElement: newElement
};
